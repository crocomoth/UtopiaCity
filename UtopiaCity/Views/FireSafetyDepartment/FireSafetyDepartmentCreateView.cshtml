@model UtopiaCity.ViewModels.FireSystem.DepartmentViewModel

@{
    ViewData["Title"] = "FireSafetyDepartmentCreateView";
}

<h1>FireSafetyDepartmentCreateView</h1>

<h4>DepartmentViewModel</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Name" class="control-label"></label>
                <input asp-for="Name" class="form-control" />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Сhief" class="control-label"></label>
                <input asp-for="Сhief" class="form-control" />
                <span asp-validation-for="Сhief" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="DepartmentStatusEnum" class="control-label"></label>
                <select asp-for="DepartmentStatusEnum" class="form-control">
                    @foreach (var status in Enum.GetValues(typeof(UtopiaCity.Models.FireSystem.ManagementSystemTransportAndEmployeess.DepartmentStatusEnum)))
                    {
                        <option value="@status">
                            @status
                        </option>
                    }
                </select>
                <span asp-validation-for="DepartmentStatusEnum" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

